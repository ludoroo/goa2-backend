"""automata — AI players and self-play tooling for the Guards of Atlantis II engine.

This package is OUR contribution; it depends on the (adopted) `goa2` engine in
process. Kept as a separate top-level package so upstream engine updates merge
cleanly without touching `src/goa2`.
"""
