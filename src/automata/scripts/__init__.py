"""Executable one-shot scripts for the automata (AI) subsystem.

This package hosts CLIs / offline pipelines that build on top of
``automata.evaluation`` and ``automata.runtime``. Each script exposes a
``main(argv)`` entry point so it is directly testable without spawning a
subprocess.
"""
